package main

import (
	"io"
	"log"
	"net/http"
	"os"
	"path"
	"path/filepath"
)

func main() {
	cwd,err := os.Getwd()
	if err != nil {
		log.Fatal(err)
	}

	http.handleFunc("/", func(w http.ResponseWriter, r *http.Request) {
		if ok, err := pat.Match("/data/*.txt", r.URL.Path); err != nil || ok {
				http.NotFound(w,r)
				return
			}
